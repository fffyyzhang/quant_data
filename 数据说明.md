# 数据说明文档

## 数据位置
所有数据存储在 `DIR_DATA` 目录下（见 `config.py`）

## 数据类型

### 1. 股票日线数据 (stock_daily)
- **位置**: `DIR_DATA/stock_daily/`
- **格式**: CSV，每个股票一个文件 `{ts_code}.csv`
- **字段**: ts_code, stock_name, trade_date, open, high, low, close, pre_close, change, pct_chg, volume, amount
- **读取**: `pd.read_csv(f"{data_dir}/{ts_code}.csv")`

### 2. ETF日线数据 (etf_daily)
- **位置**: `DIR_DATA/etf_daily/`
- **格式**: CSV，每个ETF一个文件 `{ts_code}.csv`
- **字段**: 同股票日线
- **读取**: `pd.read_csv(f"{data_dir}/{ts_code}.csv")`

### 3. 概念板块日线数据 (concept_daily)
- **位置**: `DIR_DATA/ths_concepts/`
- **格式**: CSV，每个概念一个文件 `{ts_code}.csv`
- **字段**: 同股票日线
- **读取**: `pd.read_csv(f"{data_dir}/{ts_code}.csv")`

### 4. 股票分钟数据 (stock_*min_binary)
- **位置**: `DIR_DATA/stock_{1|5|15|30}min_binary/`
- **格式**: 二进制，每个股票一个文件夹 `{ts_code}/`
  - `data.npz`: 压缩numpy数组（time[int64], open/high/low/close/volume/amount[float32]）
  - `meta.txt`: 元信息（ts_code, name, update_time, rows）
- **字段**: time, open, high, low, close, volume, amount
  - time格式: 30分钟线为 `YYYYMMDDHHMM` (int64)
- **读取**: 
  ```python
  from utils.binary_downloader import BinaryDownloader
  downloader = BinaryDownloader(data_dir=..., time_freq='30min')
  df = downloader.read_data(ts_code, as_dataframe=True)
  ```

### 5. 板块成分数据 (concept_components)
- **位置**: `DIR_DATA/concept_components/`
- **格式**: CSV，每个板块一个文件 `{ts_code}.csv`
- **字段**: ts_code, 成分股列表等
- **读取**: `pd.read_csv(f"{data_dir}/{ts_code}.csv")`

### 6. 复权因子 (adj_factor_stock/etf)
- **位置**: `DIR_DATA/adj_factor_{stock|etf}/`
- **格式**: CSV，集中式存储 `data.csv`
- **字段**: ts_code, trade_date, adj_factor
- **读取**: `pd.read_csv(f"{data_dir}/data.csv")`

## 读取原则
1. **CSV数据**: 直接使用pandas读取，注意trade_date字段格式
2. **二进制数据**: 使用对应的Downloader类的read_data方法
3. **增量更新**: 文件已存在时自动增量追加，无需手动处理
4. **时间字段**: 日线为YYYYMMDD，分钟线为YYYYMMDDHHMM
